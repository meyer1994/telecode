{
  "name": "tlg",
  "private": true,
  "scripts": {
    "build": "nitro build",
    "dev": "nitro dev",
    "prepare": "nitro prepare",
    "preview": "node .output/server/index.mjs",
    "typecheck": "pnpm prepare && pnpm cf:types && tsc --noEmit",
    "cf:types": "pnpm wrangler types ./wrangler.d.ts",
    "cf:deploy": "pnpm cf:types && pnpm build && pnpm wrangler deploy",
    "cf:db:generate": "pnpm drizzle-kit generate",
    "cf:db:migrate": "pnpm wrangler d1 migrations apply tlg",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix"
  },
  "devDependencies": {
    "@stylistic/eslint-plugin": "^5.7.0",
    "@types/node": "^25.0.6",
    "dotenv-cli": "^11.0.0",
    "drizzle-kit": "^0.31.8",
    "eslint": "^9.39.2",
    "globals": "^17.0.0",
    "nitro-cloudflare-dev": "^0.2.2",
    "nitropack": "^2.13.0",
    "typescript-eslint": "^8.52.0",
    "wrangler": "^4.58.0"
  },
  "dependencies": {
    "@grammyjs/menu": "^1.3.1",
    "@grammyjs/storage-cloudflare": "^2.4.2",
    "db0": "^0.3.4",
    "drizzle-orm": "^0.45.1",
    "grammy": "^1.39.2",
    "h3": "1.15.4"
  }
}